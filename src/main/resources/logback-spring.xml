<configuration>
    <!-- Configuración para que se muestren todos los logs en la consola -->
    <root level="INFO">
        <appender-ref ref="STDOUT" />
    </root>

    <!-- Configuración del appender de consola -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %highlight(%-5level) %logger{36} : %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Configuración para que los logs explícitos se registren en el archivo -->
    <logger name="io.github.popehiflo.invflo" level="INFO" additivity="true">
        <appender-ref ref="FILE-APPENDER" />
    </logger>

    <!-- Configuración del appender de archivo con tamaño máximo y rotación -->
    <appender name="FILE-APPENDER" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
            <fileNamePattern>app-%i.log</fileNamePattern>
            <minIndex>1</minIndex>
            <maxIndex>10</maxIndex>
        </rollingPolicy>
        <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <maxFileSize>5MB</maxFileSize>
        </triggeringPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
</configuration>
